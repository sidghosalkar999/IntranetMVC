@model IEnumerable<Zns.Intranet.Web.Models.Employee>

@{
    ViewBag.Title = "Index";
}

@{ 
    var date = DateTime.Now.ToShortDateString();
    var msg = "Welcome To Zensar";
}

<h2>Index</h2>
<bt />
@{ 
    for (int i = 0; i < 1; i++)
     {
                <h2>@date</h2>
                <h2>@msg</h2>
     }
    <a name="Link" href="www.google.com"></a>
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FieldExperience)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FieldExperience)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
